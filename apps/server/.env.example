# ===========================================
# ICA Operations Platform - Server Configuration
# ===========================================
# Copy this to .env and update with your values

# ===========================================
# REQUIRED SETTINGS
# ===========================================

# Database - PostgreSQL connection string
# For online instances:
#   - Supabase: postgresql://postgres.[project-ref]:[password]@aws-0-[region].pooler.supabase.com:5432/postgres
#   - Neon: postgresql://[user]:[password]@[endpoint].neon.tech/[database]?sslmode=require
#   - Railway: postgresql://postgres:[password]@[host].railway.app:5432/railway
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/ica_ops

# Redis - Connection string
# For online instances:
#   - Upstash: rediss://default:[password]@[endpoint].upstash.io:6379
#   - Redis Cloud: redis://default:[password]@[endpoint].redislabs.com:6379
#   - Railway: redis://default:[password]@[host].railway.app:6379
REDIS_URL=redis://localhost:6379

# JWT Authentication
# IMPORTANT: Change this to a secure random string in production!
# Generate with: openssl rand -base64 32
JWT_SECRET=your-jwt-secret-key-change-in-production
JWT_EXPIRES_IN=15m
REFRESH_TOKEN_EXPIRES_IN=7d

# Server
PORT=3001
NODE_ENV=development

# CORS - Comma-separated list of allowed origins
ALLOWED_ORIGINS=http://localhost:3000

# ===========================================
# OPTIONAL SETTINGS (Leave blank if not using)
# ===========================================

# Email Service - Resend (for sending emails)
# Get your API key at: https://resend.com
# If not set, email features will be simulated (logged to console)
RESEND_API_KEY=

# File Storage - AWS S3 (for file uploads)
# If not set, file uploads will be stored locally or simulated
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_S3_BUCKET=
AWS_REGION=
